<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <receiver
            android:name=".PluginDiscoveryReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.nightfish.lightnovelreader.PLUGIN_DISCOVERY" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".LinovelibImageProvider"
            android:authorities="io.nightfish.lightnovelreader.plugin.linovelib.images"
            android:exported="true"
            android:grantUriPermissions="true" />

    </application>
</manifest>
